### Sale Returns
---
    Strictly not allowed
        - Pending layaway
        - Cancel layaway sale
        - Pending Credit Sale
        - Void Sale
        - Rather than Regular and assembly products
        - Already returned
    Allow only return is true (Do not allow return)
        - Item is already exchanged
        - Cashback
        - Loyalty points are used
        - Item Wise Gift with purchase discount (Exclude: Allow all items returns of the original sale)
        - Item-wise promotions (Excluding: LIMITED_TO_PRODUCTS, LIMITED_TO_CATEGORIES, and Allow all items with the same group ID of the original sale)
    Allow only return is false (Do not allow return)
        - Only Return

### Sale Returns
---
    Strictly not allowed
        - Pending layaway
        - Cancel layaway sale
        - Pending Credit Sale
        - Void Sale
        - Rather than Regular and assembly products